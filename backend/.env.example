DATABASE_URL=postgresql://uasd:password@db:5432/uasd_despacho
# Genera con: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=CAMBIA_ESTO_POR_64_CARACTERES_ALEATORIOS_MINIMO
JWT_EXPIRES_IN=8h
PORT=3000
# En producción: dominio real (sin trailing slash). Múltiples: separados por coma.
CORS_ORIGIN=http://localhost:5173
